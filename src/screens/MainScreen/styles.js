import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  headerTitle: {
    color: "white",
    fontWeight: "bold",
    marginLeft: 32,
    fontSize: 18
  }
});

export default styles;
